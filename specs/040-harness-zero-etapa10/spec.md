# Spec 040: harness-zero etapa 10 — evals do harness (cap. 11)

- FR-001: suite pytest em evals/ que testa o HARNESS deterministicamente: politica, plan mode, escada de compactacao, fronteiras do subagente, schema derivado — usando um ReplayAdapter de RESPOSTAS GRAVADAS (gravacoes/*.jsonl).
- FR-002: juiz.py — LLM-as-judge atras do LLMPort (nota+justificativa por criterios); com echo demonstra o mecanismo, com chave real julga de verdade.
- FR-003: todos os evals verdes; README (10 ✅).
