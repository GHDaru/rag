# Spec 032: harness-zero etapa 02 — ToolPort (cap. 05)

- FR-001: etapa autocontida `etapas/02-tools/` evoluindo a 01: tools como funcoes Python TIPADAS; schemas DERIVADOS de assinaturas+docstrings (inspect/typing) — a cura do tedio da etapa 01.
- FR-002: ToolPort nomeado (registro + schemas() + executar()); loop reusa; chat congelado (copia da 01).
- FR-003: roda com echo (sem rede); comentarios didaticos; README atualizado (etapa 02 ✅).
