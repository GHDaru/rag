# Spec 039: harness-zero etapa 09 — subagentes (cap. 10)

- FR-001: tool task(descricao) -> sessao-FILHA (task-...) com contexto LIMPO (so a descricao), loop proprio limitado, tools RESTRITAS a leitura; so o resultado final volta ao pai.
- FR-002: filhas persistidas e visiveis em /sessions (etapa 4 pagando dividendos); politica da 06 aplicada dentro da filha.
- FR-003: smoke com adapter fake (pai delega -> filha le arquivo -> resultado volta); README (09 ✅).
